import {createStyles, makeStyles} from "@material-ui/core";

export const useStyles = makeStyles(() => {
    return createStyles({
        container: {
            marginTop: '6.7rem',
            float: 'left'
        },
    })
})
